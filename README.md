# Tinder del Arte (React + Vite + Tailwind)
- Edita `public/obras.json` para agregar/quitar obras.
- Despliega en Vercel importando este repo desde GitHub.
Scripts:
- `npm i`
- `npm run dev`
- `npm run build` / `npm run preview`
